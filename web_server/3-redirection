#!/usr/bin/env bash
# Configures an Nginx server so that /redirect_me redirects to another page
# The redirection is a "301 Moved Permanently"

sudo apt-get update
sudo apt-get -y install nginx ufw
sudo ufw allow 'Nginx HTTP'
echo "Holberton School" | sudo tee /var/www/html/index.html > /dev/null

# Add redirect rule for /redirect_me using string replacement (more reliable)
redirect_rule="\n\tlocation /redirect_me {\n\t\treturn 301 https://www.youtube.com/;\n\t}\n"
sudo sed -i "/listen 80 default_server;/a $redirect_rule" /etc/nginx/sites-available/default

sudo service nginx restart